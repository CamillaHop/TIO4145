# Copy to .env and fill in real values. Do NOT commit .env.

# Used by the optional Vercel chat function (api/chat.js)
OPENROUTER_API_KEY=

# Optional: higher-quality PDF parsing via Reducto (scripts/parse_with_reducto.py)
REDUCTO_API_KEY=

# Optional: use Anthropic as the generation provider (scripts/*)
ANTHROPIC_API_KEY=
